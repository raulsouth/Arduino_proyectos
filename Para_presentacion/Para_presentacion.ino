int numero = 0;
int tiempo = 1000;
float pi = 3.14;




long cantidad = 90000;
long resultado = cantidad * 5;







  // declaracion de la variable

void setup()  {
  Serial.begin(9600); // inicalizamos la comuncación serial
  Serial.println(numero);  // se imprime el valor de numero en monitor serial
}

/*void setup() {
  pinMode(pin, OUTPUT);  // configura el pin como salida
}*/

void loop() {
  digitalWrite(pin, HIGH);  // pone en uno el pin
  delay(1000);              // espera un segundo (1000ms)
  digitaWrite(pin, LOW);    // pone en cero el pin
  delay(1000);
}

pinMode(10, OUTPUT);
pinMode(8, INPUT);



digitalWrite(pin, estdo);


digitalWrite(10, HIGH); // enciende el led del pin 10
digitalWrite(5, LOW);   // apaga el led del pin 5
delay(1000);            // espera 1 segundo

delay(1000);
delayMicroseconds(1000);
int x = 5;

/* Esto es un bloque
de comentario
*/

  int numero = 0; 

char letra = 'a';


Serial.println(numero == 2)

  digitalWrite(pin, estado);

if(condición){
  instrucciones;
}

  if(){

  }
  else{

  }

  if(){

  }
  else if {

  }

  switch(){
    case:
    break;
  }














